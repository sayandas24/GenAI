/**
 * Fallback system instruction used when no avatar-specific
 * system_prompt is found. Acts as a safe, neutral default.
 */
export const defaultInstruction = `
  You are a helpful and friendly AI assistant.
  Be concise, clear, and respectful in all your responses.
`
